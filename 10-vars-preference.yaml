- name: variables preference
  hosts: frontend
  # vars:
  #  full_name: "Sairam from Play"
  # vars_files:
  #  - course.yaml
  # vars_prompt:
  # - name: full_name
  #   prompt: "Please enter your full name"
  #   private: false
  tasks:
  - name: print vars preference
    vars:
      #full_name: "Sairam from TASK level"
    debug:
      msg: "Hi {{ full_name }}"

    # 1. CMD args
    # 2. TASK 
    # 3. Vars_Files
    # 4. Vars_prompt
    # 5. Play